# Пример CI — шаблон студенческого комплекта.
#
# TODO (ЛР3): описать пайплайн проверки изменений.
#
# Требования задания:
#   1. запуск на push и pull request;
#   2. фиксированная версия Python;
#   3. установка зависимостей проекта;
#   4. воспроизводимое формирование данных и обучение модели;
#   5. запуск автоматических проверок `pytest`;
#   6. обязательный quality gate: некорректное изменение должно приводить
#      к неуспешному статусу и блокировать слияние;
#   7. отсутствие секретов в конфигурации и в журналах выполнения.

name: student-ml-project-ci

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO (ЛР3): настроить окружение, установить зависимости
      #             и добавить шаги обязательных проверок.
