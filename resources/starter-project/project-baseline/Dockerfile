# syntax=docker/dockerfile:1
#
# Контейнер API — шаблон студенческого комплекта.
#
# TODO (ЛР6): собрать образ сервиса инференса.
#
# Обязательные требования задания:
#   1. многостадийная сборка: зависимости и обучение отделены от runtime;
#   2. фиксированный базовый образ (например, python:3.11-slim);
#   3. установка зависимостей из pyproject.toml без кэша pip;
#   4. воспроизводимая подготовка данных и модели на стадии сборки
#      либо явно документированное монтирование готового артефакта;
#   5. непривилегированный пользователь внутри контейнера;
#   6. переменные окружения MODEL_PATH, SERVICE_VERSION и MODEL_VERSION;
#   7. HEALTHCHECK, обращающийся к /ready;
#   8. запуск uvicorn с приложением mlops_course.api:app на порту 8000;
#   9. отсутствие секретов в слоях образа и в истории сборки.
#
# Ненужные файлы исключаются через .dockerignore.

FROM python:3.11-slim AS builder

# TODO (ЛР6): установить зависимости и подготовить артефакт модели.

FROM python:3.11-slim AS runtime

# TODO (ЛР6): скопировать зависимости, код, контракт и модель,
#             настроить пользователя, переменные окружения и HEALTHCHECK.

EXPOSE 8000

# TODO (ЛР6): указать команду запуска сервиса.
