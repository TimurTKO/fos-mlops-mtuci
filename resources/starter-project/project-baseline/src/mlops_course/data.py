"""Загрузка контракта данных и проверяемая загрузка датасета.

Студенческий каркас. Реализация выполняется в ЛР1 и расширяется в ЛР2.
Сигнатуры функций менять не следует: на них опираются обучение, инференс,
API и автоматические проверки.
"""

from __future__ import annotations

import json
from pathlib import Path

import pandas as pd


def load_contract(path: str | Path) -> dict:
    """Прочитать контракт данных из JSON-файла и вернуть его как словарь.

    Контракт описывает список признаков `feature_columns`, имя целевой
    колонки `target_column` и допустимые значения `target_values`.
    """
    # TODO (ЛР1): прочитать файл в кодировке utf-8 и разобрать его как JSON.
    raise NotImplementedError("ЛР1: реализуйте загрузку контракта данных")


def load_dataset(path: str | Path, contract: dict, require_target: bool = True) -> pd.DataFrame:
    """Загрузить CSV-файл и проверить его соответствие контракту данных.

    Ожидаемое поведение, проверяемое тестами ЛР2:

    1. отсутствие обязательной колонки — `ValueError`;
    2. нечисловой тип признака — `TypeError`;
    3. пропуски в признаках — `ValueError`;
    4. недопустимое значение целевой переменной — `ValueError`;
    5. при `require_target=False` целевая колонка не требуется.

    Файл `data/incident_bad_schema.csv` не должен проходить проверку.
    """
    # TODO (ЛР1): загрузить CSV в DataFrame.
    # TODO (ЛР2): проверить состав колонок, типы, пропуски и значения цели.
    raise NotImplementedError("ЛР1–ЛР2: реализуйте проверяемую загрузку датасета")
